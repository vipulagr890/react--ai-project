import React from "react";

const Rank = () => {
  return (
    <div style={{ color: "white" }}>
      <div
        style={{ fontSize: "1.6rem" }}
      >{`Vipul, your current rank is...`}</div>
      <div style={{ fontSize: "2rem" }}>{`#5`}</div>
    </div>
  );
};

export default Rank;
